import { ApiController, Request, Response } from '@app/core';
import { Controller, Get, Req, Res } from '@nestjs/common';
import { <%= className %>Service } from '../services';

@Controller('users')
export class <%= className %>Controller extends ApiController {
  constructor(private <%= name %>s: <%= className %>Service) {
    super();
  }
}
