import { Transformer } from '@app/core';

export class <%= filename %>Transformer extends Transformer {
  availableIncludes = [];
  defaultIncludes = [];

  async transform(obj: Record<string, any>):  Promise<Record<string, any>>{
    return {};
  }
}
